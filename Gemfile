source "https://rubygems.org"

# GitHub Pages — pins Jekyll + whitelisted plugins to what GH Pages runs in safe mode.
# Local preview: `bundle install && bundle exec jekyll serve`
gem "github-pages", group: :jekyll_plugins
gem "webrick" # needed for local serve on Ruby 3+
