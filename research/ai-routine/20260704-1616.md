# AI routine research note

- Run timestamp: 2026-07-04 16:16 KST
- Phase: bounded refinement pass over older published cards

## Reviewed

- `kmp-ai — KMP로 Android·iOS·Desktop에서 오프라인 LLM을 실행하는 llama.cpp 바인딩(Maven Central)`

## Sources checked

- https://github.com/fadizg/kmp-ai
- https://api.github.com/repos/fadizg/kmp-ai

## Signals checked

- `kmp-ai` README still supports the core AI+Android lesson: Kotlin Multiplatform, llama.cpp, JVM/Android/iOS coverage, `Flow<Token>` streaming, model loading, SHA-256 verification, resumable downloads, KV-cache reuse, GBNF grammars, and Compose Multiplatform sample coverage.
- `kmp-ai` README still supports the deployment lesson that Maven Central ships prebuilt Android `.so` files so consumers do not need to provision an NDK just to use the AAR.
- GitHub API verification during this run reported `kmp-ai` at 0 stars, 0 forks, not archived, and a recent push on 2026-05-06T19:18:41Z, so the repo is live but lacks a strong popularity/adoption signal.

## Result

- Refined the existing `kmp-ai` card for readability and evidence hygiene.
- Published no new card in this run.

## Selection / rejection

- Selected `kmp-ai` because the current public README still supports a clearer explanation of the KMP + llama.cpp + Maven Central lesson.
- Rejected new-card publication because the fresh topic search did not surface a better candidate than the already-published nearby cards, and `kmp-ai` itself lacks a strong live popularity signal.
- Rejected pure Android-only follow-ups because they remain below the AI-centered threshold for this routine.

## Confidence

- Medium

## Notes

- No app-factory-android issues, branches, or pull requests were touched.
- No GitHub Actions were added.
