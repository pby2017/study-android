# AI card routine research note — 2026-06-23 08:43 KST

## Scope

- Repository-only edit for `study-android`.
- No GitHub Actions, issues, branches, PRs, comments, commits, or pushes.
- Pure Android topics rejected unless the core lesson is AI+Android developer education.
- Wrapper owns final verification, commit, and push.

## Sync / resume state

- `git status --short --branch`: clean before sync.
- `git pull --ff-only origin master`: already up to date.
- No `research/ai-routine/resume.md` checkpoint was present at run start.

## Existing duplicate checks

Checked `_data/cards.yml`, `_data/ai_queue.yml`, `auto-post-log.md`, and older `research/ai-routine/*.md` notes with `rg`.

Rejected as duplicate / below threshold:

- `AppFunctions / Android MCP`
  - Sources checked:
    - https://android-developers.googleblog.com/2026/06/Android-17.html
    - https://developer.android.com/ai/appfunctions
  - Reason: existing cards already cover `AppFunctions(Android MCP)` and `Android 17 AppFunctions Agent Skill`.
- `Google Tensor SDK Beta with LiteRT`
  - Source checked:
    - https://developers.googleblog.com/google-tensor-sdk-beta-with-litert/
  - Reason: existing card `Tensor SDK Beta + LiteRT — Pixel NPU 배포를 하나의 경로로 묶기`.
- `Android XR + Gemini Geospatial Tour Guide`
  - Source checked:
    - https://android-developers.googleblog.com/2026/06/android-xr-geospatial-api-gemini.html
  - Reason: existing card `Android XR + Gemini — Geospatial 좌표를 구조화 JSON 투어로 바꾸는 패턴`.
- Pure Android 17 release coverage without an AI tool/agent angle
  - Source checked:
    - https://android-developers.googleblog.com/2026/06/Android-17.html
  - Reason: below threshold for this routine unless tied to Android MCP/AppFunctions or NPU AI deployment, both already covered.

## Selected card

### Copilot CLI LSP Setup skill

- Published title: `Copilot CLI LSP 설정 — 에이전트에게 IDE식 코드 지능을 붙이기`
- Source checked:
  - https://github.blog/ai-and-ml/github-copilot/give-github-copilot-cli-real-code-intelligence-with-language-servers/
- Evidence:
  - GitHub Blog, 2026-06-10, describes installing and configuring LSP servers for GitHub Copilot CLI.
  - Source says the LSP Setup skill replaces brute-force grep/decompile patterns with language-server code intelligence.
  - Source states LSP provides definition, references, type resolution, hover documentation, and structured symbol answers.
  - Source states the skill includes curated server data for 14 languages and supports user-level or repository-level configuration.
  - Source states verification checks the server binary and JSON config.
- Popularity/adoption signal:
  - GitHub official Copilot source; practical setup path for Copilot CLI.
- Developer value:
  - Teaches that coding-agent reliability depends on structured code-intelligence tools, not only prompt quality or model choice.
- Exact metrics:
  - No star counts used.
- Confidence: high.

## Refinement pass

Cursor source:

- `research/ai-routine/refinement-cursor.md`

Reviewed older cards first:

- `서브에이전트 — 컨텍스트를 지키는 분업`

Sources re-checked:

- https://code.claude.com/docs/en/sub-agents

Verification:

- Source supports separate context windows, custom system prompts, specific tool access, independent permissions, and summary return.
- Source supports project `.claude/agents/` and user `~/.claude/agents/` locations, with closer project definitions taking precedence for nested duplicate names.
- Source supports `tools`, `disallowedTools`, MCP scoping, model routing, and `CLAUDE_CODE_SUBAGENT_MODEL` precedence.

Before/after rationale:

- Before: card accurately described context-preserving delegation and allow/deny tool controls.
- After: summary now makes the operational lesson clearer: scope precedence, MCP/tool isolation, and model routing are the practical design levers.

Next cursor:

- Continue with `Claude Code GitHub Action — PR에 부르면 자동 수정·리뷰`.

## Files changed

- `_data/cards.yml`
- `auto-post-log.md`
- `research/ai-routine/refinement-cursor.md`
- `research/ai-routine/20260623-084306.md`

## Verification performed

The wrapper owns commit and push. Local pre-wrapper checks run in this session:

```bash
ruby -c scripts/auto_post.rb
ruby -e 'require "yaml"; YAML.load_file("_data/cards.yml"); YAML.load_file("_data/ai_queue.yml"); puts "yaml_ok"'
git diff --check
```

Results:

- `ruby -c scripts/auto_post.rb`: Syntax OK.
- YAML load for `_data/cards.yml` and `_data/ai_queue.yml`: `yaml_ok`.
- `git diff --check`: passed with no output.
- Duplicate-title sanity check: 581 cards; no duplicate title printed for the new Copilot CLI LSP card.
