# Local AI card routine — 2026-06-27 22:24 KST

## Scope

- Repo-only edit pass for study-android cards.
- No GitHub Actions, commits, pushes, issues, branches, or PR actions.
- Pure Android topics rejected; Android candidates qualify only with a clear AI+Android developer-education angle.

## Fresh candidates considered

| Candidate | Sources checked | Evidence signal | Decision |
| --- | --- | --- | --- |
| ADK for Kotlin / ADK for Android 0.1.0 | Existing `_data/cards.yml` and prior audit notes; official Google Developers Blog history already recorded in earlier runs | AI+Android in scope, but repo already has ADK for Android, ADK long-running agents, ADK+A2A, Gemini Nano, AppFunctions, and hybrid inference cards | Rejected as duplicate/saturated |
| Gemini Nano / Gemma / LiteRT / AI Edge Gallery follow-ups | Existing `_data/cards.yml` and prior audit notes | AI+Android in scope, but existing cards cover Gemma 4, Gemini Nano 4, LiteRT-LM, Tensor SDK + LiteRT, AI Edge Gallery MCP, ML Kit GenAI, Firebase AI Logic, and mobile agent tooling | Rejected as duplicate/saturated |
| Broad GitHub Copilot / Codex / Claude Code follow-ups | Existing `_data/cards.yml`, recent auto-post log | AI developer relevance exists, but recent cards already cover Agent View, Codex Remote, Copilot app/automations, Agent Finder/ARD, strict marketplaces, and harness evaluation | Rejected as duplicate/overlap |
| Pure Android platform topics | Scope boundary | No central AI angle | Rejected below threshold |

## Refinement pass

### Reviewed: MCP Tool Search

- Card: `MCP Tool Search — MCP 서버 수십 개 붙여도 컨텍스트가 안 터지는 'on-demand 로딩'(Claude Code 기본값)`
- Source re-checked: https://code.claude.com/docs/en/mcp
- Public-source signals:
  - Claude Code docs state tool search is enabled by default and defers MCP tool definitions until needed.
  - Docs define `ENABLE_TOOL_SEARCH` modes, Vertex/proxy fallback behavior, model `tool_reference` limits, and `alwaysLoad`.
- Decision: skipped. The existing card already captures the default behavior, configuration modes, model/proxy caveats, and server-instructions lesson clearly.

### Refined: DeepSWE

- Card: `DeepSWE(Datacurve) — 채점기 오류를 줄이고 '오래 걸리는 일'로 다시 줄 세운 코딩 에이전트 벤치마크`
- Source re-checked: https://deepswe.datacurve.ai/blog/deepswe-v1-1
- Public-source signals:
  - DeepSWE v1.1 keeps 113 long-horizon tasks and updates execution/grading.
  - The benchmark evaluates committed patches in a clean verifier container.
  - CTRF reports expose task-defining test names and statuses.
  - Current page reports Claude Fable 5 70%±4, GPT-5.5 67%±6, Claude Opus 4.8 59%±2 at the top of the displayed leaderboard.
- Before/after rationale:
  - Before: dense name/metric cluster with too many adjacent benchmark references.
  - After: clearer educational point: benchmark quality depends on task shape, committed-diff grading, isolated verification, and cost/token/step context, not model name alone.
- Confidence: high.

## Published card decision

- Published new card: none.
- Reason: no fresh candidate cleared both the non-duplicate gate and exact public-source claim support in this bounded run.

## Verification plan

- `ruby -c scripts/auto_post.rb`
- `ruby -e 'require "yaml"; YAML.load_file("_data/cards.yml"); YAML.load_file("_data/ai_queue.yml"); puts "yaml_ok"'`
- `git diff --check`
