# AI/Android card routine research — 2026-06-24 19:58 KST

## Run constraints

- Used repo skill `.codex/skills/local-ai-card-routine/SKILL.md`.
- Synced before edits: `git pull --ff-only origin master` returned already up to date.
- Scope: AI and AI+Android developer education only.
- Explicit exclusions honored: no GitHub Actions, no app-factory-android issues/branches/PRs/comments/triage, no commit, no push.
- Wrapper script owns final verification, commit, and push.

## Older-card refinement pass

Cursor file: `research/ai-routine/refinement-cursor.md`.

Reviewed oldest-first from the cursor:

1. `Dreams(Anthropic) — 에이전트가 세션 사이에 '잠자며' 메모리를 정리하는 리서치 프리뷰`
   - Source re-checked: https://platform.claude.com/docs/en/managed-agents/dreams
   - Fact-check signals: Anthropic Managed Agents docs state Dreaming is a research preview; a dream reads one existing memory store plus 1 to 100 sessions; it produces a separate output memory store; the input store is never modified; status can be pending/running/completed/failed/canceled; partial output may remain after failure or cancellation; `instructions` steer synthesis rather than exact line edits; billing uses standard API token rates.
   - Before/after rationale: the original card was accurate but dense. The refined card now makes the operational boundary clearer: immutable input store, reviewable output store, future-session attachment, partial-output inspection, and the limit of `instructions`.
   - Confidence: high. Central claims are directly supported by official Anthropic docs.

2. `OpenClaw — 메신저로 부리는 self-hosted·모델 비종속 에이전트(MIT)`
   - Source re-checked: https://github.com/openclaw/openclaw
   - Additional live metric source checked: GitHub API for `openclaw/openclaw`.
   - Exact metrics verified during this run: 380,229 stars, 79,633 forks, pushed at 2026-06-24T10:58:22Z.
   - Fact-check signals: current README describes OpenClaw as a personal AI assistant run on your own devices; lists WhatsApp, Telegram, Slack, Discord, Google Chat, Signal, iMessage, Teams, Matrix, LINE, WeChat, and other channels; describes Gateway as the control plane; recommends `openclaw onboard --install-daemon`; mentions OpenAI ChatGPT/Codex OAuth subscriptions and many providers/models; README badge shows MIT.
   - Before/after rationale: removed unsupported host-tool/sandbox phrasing from the card, made the channel/Gateway/onboard lesson more readable, and replaced approximate star wording with live same-run metrics.
   - Confidence: high for README/API claims; medium for license wording because GitHub API returned `NOASSERTION` while the README badge says MIT.

3. `ccusage — 코딩 에이전트 토큰·비용을 로컬 로그만으로 집계하는 CLI(16.2k★)`
   - Source re-checked: https://github.com/ccusage/ccusage
   - Additional live metric source checked: GitHub API for `ccusage/ccusage`.
   - Exact metrics verified during this run: 16,517 stars, 685 forks, pushed at 2026-06-24T09:18:46Z.
   - Fact-check signals: current README documents `npx ccusage@latest`, `bunx ccusage`, daily/weekly/monthly/session reports, source-focused reports for Claude Code, Codex, OpenCode, Amp, Droid, Codebuff, Hermes, Goose, OpenClaw, Kilo, Kimi, Qwen, Copilot CLI, Gemini CLI, plus `blocks`, `statusline`, `--json`, `--breakdown`, date filters, `--offline`, and custom pricing overrides.
   - Before/after rationale: updated the card from a Claude-centric cost tool description to the current unified multi-agent usage-reporting surface, and replaced approximate star wording with same-run metrics.
   - Confidence: high. Central claims are supported by current README and live GitHub API metrics.

Cursor updated to continue with `Claude Code on the web — 터미널 안 열고 브라우저·모바일에서 작업을 클라우드에 던지기`.

## Fresh candidates considered

### Rejected: ADK for Kotlin / ADK for Android 0.1.0

- Source checked: https://developers.googleblog.com/adk-kotlin-android-building-ai-agents/
- Reason rejected: AI+Android is in scope, but this is duplicate. `_data/cards.yml` already contains `ADK for Android`, `ADK for Android 0.1.0`, and `ADK 장기 실행 에이전트`; recent notes also repeatedly rejected this angle as already covered.

### Rejected: Google AI Edge Gallery / AI Edge Gallery MCP

- Sources checked: https://developers.google.com/edge and https://github.com/google-ai-edge/gallery
- Reason rejected: AI+Android is in scope, but duplicate. Existing cards already cover AI Edge Gallery MCP, Gemma 4/LiteRT-LM, AICore, ML Kit GenAI, Firebase AI Logic, and related on-device Android AI deployment angles.

### Rejected: GitHub third-party coding-agent security validation

- Source checked: https://github.blog/changelog/2026-06-09-security-validation-for-third-party-coding-agents/
- Reason rejected: duplicate. Existing card already covers `GitHub third-party agent security validation — Codex·Claude PR도 CodeQL·의존성·secret 검사`.

### Rejected: GitHub Copilot June 2026 changelog items

- Source checked: https://github.blog/changelog/month/06-2026/
- Reason rejected: duplicate or below threshold for this run. Existing cards and notes already cover Copilot BYOK models, Claude as agent provider / JetBrains updates, Copilot usage metrics `ai_credits_used`, MAI-Code-1-Flash, Copilot code review AGENTS.md support, GitHub agent apps, Copilot CLI steering, and Copilot LSP setup.

### Rejected: pure Android platform topics

- Reason rejected: below threshold. Pure Android topics are out of scope unless the core angle is AI+Android educational content.

## Published changes

- Refined 3 older AI cards:
  - `Dreams(Anthropic) — 에이전트가 세션 사이에 '잠자며' 메모리를 정리하는 리서치 프리뷰`
  - `OpenClaw — 메신저로 부리는 self-hosted·모델 비종속 에이전트(MIT)`
  - `ccusage — 코딩 에이전트 토큰·비용을 로컬 로그만으로 집계하는 CLI(16.2k★)`
- Added 0 new cards. The fresh candidates checked in this bounded run were duplicate or below threshold.

## Verification

Completed after edits:

```bash
ruby -c scripts/auto_post.rb
# Syntax OK

ruby -e 'require "yaml"; YAML.load_file("_data/cards.yml"); YAML.load_file("_data/ai_queue.yml"); puts "yaml_ok"'
# yaml_ok

git diff --check
# passed with no output
```
