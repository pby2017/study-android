# Local AI card routine — 2026-06-28 16:57 KST

## Start state

- Used repo skill: `.codex/skills/local-ai-card-routine/SKILL.md`.
- Synced first: `git pull --ff-only origin master` reported already up to date.
- `research/ai-routine/resume.md`: not present.
- Allowed edit surface only: `_data/cards.yml`, `_data/ai_queue.yml`, `auto-post-log.md`, and `research/ai-routine/*`.
- No GitHub Actions, commits, pushes, external issues, branches, or PRs were created.

## Fresh candidate gate

| Candidate | Sources / duplicate checks | Decision |
| --- | --- | --- |
| Codex App Server / Codex Sites / Codex Remote / Appshots follow-ups | Existing `_data/cards.yml`, `auto-post-log.md`, and prior routine notes already cover these Codex app surfaces. | Rejected as duplicate/overlap. |
| Claude Code Agent View / background sessions | Existing card published 2026-06-27. | Rejected as duplicate. |
| Cursor Customize / SDK / Automations / Bugbot follow-ups | Existing cards and recent rejected notes cover Cursor Customize, SDK custom tools, auto review, and related team settings. | Rejected as duplicate/overlap. |
| ADK for Android, Gemini Nano/Gemma/LiteRT, Firebase AI Logic, AppFunctions, Android Bench | Existing `_data/cards.yml` already has multiple AI+Android cards for these angles. Pure Android variants remain below threshold. | Rejected as duplicate/saturated. |
| OpenAI agent-work usage research | Existing `Codex 경제 리서치` / OpenAI Shift to Agentic coverage and recent routine notes. | Rejected as duplicate. |

Published: none. Current fresh candidates were duplicate, saturated, or below the exact-source gate.

## Older-card refinement pass

Cursor source: `research/ai-routine/refinement-cursor.md`

### Refined

`Kiro(AWS) — '코드가 아니라 스펙이 산출물'이라며 requirements→design→tasks 3단계를 강제하는 Spec-Driven 에이전트`

Sources re-checked:
- https://kiro.dev/docs/specs/
- https://kiro.dev/docs/

Public-source support:
- Kiro public docs describe Kiro as an agentic IDE for spec-driven development.
- Specs page supports requirements/design/tasks, Feature Specs, Bugfix Specs, Quick Plan, task execution status, dependency graph, and wave-based parallel task execution.
- Docs navigation publicly exposes IDE, CLI, Web, and Mobile product surfaces, but this refinement avoids unsupported implementation claims beyond the Specs page.

Before/after rationale:
- Removed stale or unsupported model/runtime details from the old card.
- Kept the original Kiro/spec-driven topic.
- Improved readability around the practical lesson: specs are execution boundaries and task queues, not only documentation.

## Next refinement position

Continue after `Kiro(AWS) — '코드가 아니라 스펙이 산출물'이라며 requirements→design→tasks 3단계를 강제하는 Spec-Driven 에이전트`; next likely older candidate is `cc-sdd(Spec-Driven Development) — Kiro의 '스펙→설계→태스크' 워크플로우를 아무 코딩 에이전트에서 쓰기(오픈소스)`.

## Verification

Passed:

```bash
ruby -c scripts/auto_post.rb
ruby -e 'require "yaml"; YAML.load_file("_data/cards.yml"); YAML.load_file("_data/ai_queue.yml"); puts "yaml_ok"'
git diff --check
```
