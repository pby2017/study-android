# AI card routine — 2026-06-24 05:25 KST

## Scope and constraints

- Repository-only edit run for `study-android`.
- No GitHub Actions, no commits, no pushes.
- No app-factory-android issues, branches, pull requests, comments, or triage.
- Pure Android topics rejected unless the central lesson is AI+Android educational card content.
- Started from a clean worktree and synced with `git pull --ff-only origin master` before edits.
- No `research/ai-routine/resume.md` checkpoint was present at start.
- Refinement pass followed `research/ai-routine/refinement-cursor.md` and reviewed older cards first.

## Fresh candidates considered

### Selected: Copilot CLI GA terminal interface

- Source checked:
  - https://github.blog/changelog/2026-06-23-copilot-cli-new-terminal-interface-is-generally-available/
- Primary/reputable signal:
  - Official GitHub Changelog release dated 2026-06-23.
- Exact claims used:
  - The redesigned Copilot CLI terminal interface is generally available after a Microsoft Build 2026 preview.
  - Interactive sessions can show Session, Gists, Issues, and Pull requests tabs.
  - Highlighted issues or pull requests can be inserted into the prompt as references for investigation, fixes, comments, or review.
  - `/mcp add` and `/mcp search` configure MCP servers in-session; servers are usable without restarting the CLI.
  - `/skills`, `/plugin`, and `/settings` expose skills, plugins, and settings inside the CLI session.
- Developer value:
  - Shows that terminal coding agents are becoming operational control surfaces: work selection, context references, MCP setup, skills, plugins, and accessibility matter alongside model quality.
- Duplicate check:
  - Existing cards cover Copilot CLI steering, LSP setup, selective delegation, JetBrains updates, BYOK, sandboxes, and custom agents.
  - No existing card covered the 2026-06-23 GA terminal interface or its tabbed GitHub-work + in-session MCP/skills/plugins setup angle.
- Confidence: high.

### Rejected: ADK for Kotlin / ADK for Android 0.1.0

- Source checked:
  - https://developers.googleblog.com/adk-kotlin-android-building-ai-agents/
- Reason rejected:
  - In scope as AI+Android, but duplicate. Existing cards and prior notes already cover ADK for Android, ADK for Android 0.1.0, and ADK long-running-agent state patterns.

### Rejected: Android 17 / AppFunctions follow-ups

- Sources checked:
  - https://android-developers.googleblog.com/2026/06/Android-17.html
  - https://developer.android.com/ai/appfunctions
- Reason rejected:
  - In scope only when framed around AI+Android, but duplicate for this run. Existing cards already cover AppFunctions(Android MCP), Android 17 AppFunctions Agent Skill, and related Android MCP/AppFunctions angles.

### Rejected: Codex Security plugin June changelog

- Source checked:
  - https://developers.openai.com/codex/security/plugin/changelog
- Reason rejected:
  - Valid AI developer topic, but too close to the existing broader `Codex Security — AI 보안 자동화는 권한 등급과 격리 검증이 먼저다` card for this run. A future card could be justified only with a narrower portable-artifact/backlog-triage lesson.

### Rejected: pure Android-only topics

- Reason:
  - Below threshold for this routine unless the core angle is AI+Android developer education.

## Refinement pass

### Refined: Cursor 코드베이스 인덱싱 — 임베딩으로 '의미 검색'하되 원문은 서버에 안 남긴다

- Source re-checked:
  - https://cursor.com/docs/agent/tools/search
  - https://cursor.com/docs/context/semantic-search, redirected to the current Search docs.
- Before:
  - The card used an older source URL and included an older accuracy improvement claim that was not present in the current public source.
- After:
  - Refreshed the source URL to the current Search docs.
  - Removed the unsupported accuracy metric.
  - Clarified current source-backed behavior: meaningful code chunks, vector embeddings, automatic indexing, 80% search availability, 5-minute changed-file sync, `.gitignore`/`.cursorignore`, encrypted paths, no plaintext source-code storage, semantic search plus grep, and Explore subagent for broad searches.
- Rationale:
  - The original topic remains the same, but the current docs support a clearer practical lesson: use grep for exact symbols/strings, semantic search for behavior/concepts, and subagents for broad exploration without flooding the main context.
- Confidence: high.

## Published changes prepared

- Added 1 new card:
  - `Copilot CLI GA: 터미널 에이전트도 이슈·MCP·스킬을 한 화면에서 다룬다`
- Refined 1 existing card:
  - `Cursor 코드베이스 인덱싱 — 임베딩으로 '의미 검색'하되 원문은 서버에 안 남긴다`
- Updated:
  - `_data/cards.yml`
  - `auto-post-log.md`
  - `research/ai-routine/refinement-cursor.md`
  - `research/ai-routine/20260624-052515.md`

## Verification

Passed:

```bash
ruby -c scripts/auto_post.rb
ruby -e 'require "yaml"; YAML.load_file("_data/cards.yml"); YAML.load_file("_data/ai_queue.yml"); puts "yaml_ok"'
git diff --check
```

Observed output:

- `Syntax OK`
- `yaml_ok`
- `git diff --check` produced no output
- `_data/cards.yml` card count: 549
