# Local AI card routine — 2026-06-27 18:17 KST

## Scope

- Repository-only run for `study-android`.
- No GitHub Actions, app-factory-android issues, branches, PRs, comments, or triage.
- Pure Android-only candidates rejected below threshold.
- Wrapper owns final commit and push; this run did not commit or push.

## Fresh candidates considered

| Candidate | Sources checked | Evidence / signal | Decision |
| --- | --- | --- | --- |
| Generic queue item: Agent Teams | `_data/cards.yml`, prior notes, `https://code.claude.com/docs/en/agent-teams` | Valid AI topic, but existing cards already cover Agent Teams and broader multi-agent tiers. | Rejected as duplicate/low marginal value. |
| Generic queue item: OpenAI GPT-5.4 model selection | `_data/cards.yml`, prior notes, `https://platform.openai.com/docs/models` | AI topic, but queue claim is broad operating advice already covered by model-routing, eval, and harness cards. | Rejected as duplicate/weak exact-source support. |
| Recent Copilot / BYOK / Agent Finder / ARD follow-ups | Existing `_data/cards.yml`, `auto-post-log.md`, prior routine notes | Official-source topics are strong, but this repo already has cards covering Copilot app, BYOK, Agent Finder/ARD, runtime discovery, plugin standards, and harness efficiency. | Rejected as duplicate/overlap. |
| Broad Android AI / Gemma / ADK / Android benchmark follow-ups | Existing `_data/cards.yml`, prior notes | AI+Android can qualify, but current broad follow-ups overlap existing Gemma/ADK/Android Bench cards or lack a new exact educational angle. | Rejected as duplicate/saturated; pure Android-only variants below threshold. |

No fresh card was published. The duplicate surface was too high, and the routine should not force weak cards.

## Refinement pass

### Agent Client Protocol(ACP) — '에디터'와 '코딩 에이전트'를 분리하는 개방형 표준(Zed·JetBrains)

- Sources re-checked:
  - https://zed.dev/acp
  - https://agentclientprotocol.com/get-started/introduction.md
  - https://agentclientprotocol.com/protocol/v1/overview.md
  - https://agentclientprotocol.com/get-started/registry.md
- Public-source support:
  - Zed ACP page describes connecting any ACP-speaking agent to a purpose-built IDE interface.
  - ACP introduction says the protocol standardizes communication between code editors/IDEs and coding agents, suitable for local and remote scenarios.
  - ACP overview specifies JSON-RPC 2.0, initialization, authentication, session creation/loading, prompt turns, session updates, permission requests, filesystem, terminal, and extensibility.
  - Introduction states local agents use JSON-RPC over stdio as editor subprocesses; remote agents can use HTTP or WebSocket, with full remote support still in progress.
  - Registry docs describe a curated set of ACP-compatible agents that support authentication.
- Before/after rationale:
  - Kept the original interoperability lesson.
  - Replaced older Zed/JetBrains-heavy framing with current protocol-level details: Client/Agent split, JSON-RPC lifecycle, local/remote transport boundary, and ACP Registry.
  - Avoided unsupported adoption metrics or star counts.
- Confidence: high.

## Publication log summary

- Published: none.
- Refined: `Agent Client Protocol(ACP) — '에디터'와 '코딩 에이전트'를 분리하는 개방형 표준(Zed·JetBrains)`.
- Rejected: queue items and recent official follow-ups as duplicate/overlap; pure Android-only topics below threshold.
