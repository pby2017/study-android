# AI card routine run — 2026-06-24 09:33 KST

## Scope

- Repository-only content update for `study-android`.
- No GitHub Actions, app-factory-android issues, branches, PRs, comments, commits, or pushes.
- Topic gate: AI or AI+Android developer education only; pure Android rejected.

## Sync and state

- `git status --short --branch`: clean on `master...origin/master`.
- `git pull --ff-only origin master`: already up to date.
- `research/ai-routine/resume.md`: absent, so no pending checkpoint to resume.
- Refinement cursor started after `Continue CLI(cn) — 모델·규칙·도구를 끼워 쓰는 모듈형 오픈소스 터미널 에이전트`.

## Candidates considered

### Selected: Reusable prompts deprecation

- Sources checked:
  - https://developers.openai.com/api/docs/deprecations
  - https://developers.openai.com/api/docs/guides/prompt-engineering
  - https://developers.openai.com/api/docs/guides/prompting/migrate-from-prompt-object
- Evidence:
  - OpenAI deprecations page says reusable prompt objects were announced deprecated on 2026-06-03.
  - Same page schedules `v1/prompts` API and reusable prompt objects shutdown for 2026-11-30.
  - Prompt engineering guide says production prompts should live in application code instead of reusable prompt objects.
  - Guide recommends typed function arguments/schemas for dynamic values, passing generated `instructions` and `input` to Responses API, and adding fixtures/tests/evaluation checks before production prompt changes.
- Popularity/adoption signal:
  - Official OpenAI API deprecation and migration guidance; directly affects developers using prompt objects.
- Duplicate check:
  - Existing `AgentKit 종료 공지` card covers Agent Builder and Evals shutdown, not reusable prompt objects / `v1/prompts`.
- Selected reason:
  - Current, official, actionable AI developer operations topic with exact migration implications.
- Confidence: high.

### Rejected: Android 17 general release coverage

- Sources checked:
  - https://android-developers.googleblog.com/2026/06/Android-17.html
  - https://developer.android.com/ai
- Rejected reason:
  - The broad Android 17 release story is mostly platform/product news. AI subtopics such as AppFunctions or on-device AI are already covered in recent cards, and a general Android release card would violate the pure-Android boundary.
- Confidence: high.

### Rejected: OpenAI Evals to Promptfoo migration as a new card

- Sources checked:
  - https://developers.openai.com/api/docs/deprecations
  - https://developers.openai.com/cookbook/examples/evaluation/moving-from-openai-evals-to-promptfoo
- Rejected reason:
  - Exact topic overlaps with the existing Agent Builder/Evals shutdown card and the previously refined promptfoo card.
- Confidence: high.

## Refinement pass

### Refined: Continuous Claude — Ralph 루프에 PR·CI·머지까지 붙인 '무인 개발 파이프라인'

- Sources re-checked:
  - https://github.com/AnandChowdhary/continuous-claude
- Current facts verified:
  - GitHub page showed MIT license, 1.4k stars, 92 forks, and README support for Claude Code CLI and Codex CLI providers.
  - README describes the loop: create branch, run selected AI coding agent, create PR through GitHub CLI, monitor checks/reviews, merge on success, discard on failure, then repeat.
  - README documents `SHARED_TASK_NOTES.md` as the continuity file and `--worktree` for parallel worktrees.
  - README documents GitHub PR automation as the supported full automation path and says self-hosted forges such as Gitea are not supported yet.
  - README lists safety/bounding flags including `--dry-run`, `--max-runs`, `--max-duration`, `--max-cost`, `--stall-threshold`, `--disable-commits`, and `--disable-branches`.
- Before/after rationale:
  - Before: useful but mixed the star count into a "claim" phrase and emphasized local-only mode more than current bounding controls.
  - After: clearer developer lesson around PR lifecycle automation, continuity notes, provider support, worktrees, and bounded trial runs.
- Scope check:
  - AI developer workflow topic; no app feature request and no pure Android topic.

## Published changes

- Added 1 new AI card:
  - `Reusable prompts 종료: 프롬프트도 코드 리뷰·테스트 대상이 된다`
- Refined 1 older AI card:
  - `Continuous Claude — Ralph 루프에 PR·CI·머지까지 붙인 '무인 개발 파이프라인'`

## Verification

Results after edits:

```bash
ruby -c scripts/auto_post.rb
# Syntax OK

ruby -e 'require "yaml"; YAML.load_file("_data/cards.yml"); YAML.load_file("_data/ai_queue.yml"); puts "yaml_ok"'
# yaml_ok

git diff --check
# pass, no output
```

Additional check:

```bash
ruby -e 'require "yaml"; cards=YAML.load_file("_data/cards.yml")["cards"]; puts cards.size; puts cards.last["title"]'
# 551
# Reusable prompts 종료: 프롬프트도 코드 리뷰·테스트 대상이 된다
```
