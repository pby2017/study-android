<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.day06">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".practice01.ChatRoomListActivity"></activity>
        <activity android:name=".example04.WebViewActivity" />
        <activity android:name=".example03.ActionBarActivity02" />
        <activity android:name=".example03.ActionBarActivity" />
        <activity android:name=".example02.TabHostActivity" />
        <activity android:name=".example01.ViewFlipperActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>