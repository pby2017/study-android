<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.day05">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".example05.SlidingDrawerActivity"></activity>
        <activity android:name=".example05.ScrollActivity" />
        <activity android:name=".example04.BarActivity" />
        <activity android:name=".example03.AutoCompleteActivity" />
        <activity android:name=".practice01.SelectPickerActivity" />
        <activity android:name=".example02.PickerActivity" />
        <activity android:name=".example01.ClockActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>